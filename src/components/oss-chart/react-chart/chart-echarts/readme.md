# **大佬提的需求**
## 静态文件
1. 配置文件
   - 地图的json配置文件,支持现场自行维护
   - 坐标点位置这些配置信息都需要现场维护
2. 地图资源
   - echarts的地图文件，也要放到公共位置
## 请求
- 请求地图的公共配置，先把一些公共配置加载进来，作为资源存储起来

## 样式
- 每个项目需要的地图气泡不同
  - 直接外部传个option进来，整个替换原来的
  - 咱们支持不同的气泡配置，目前需要的是全国、各省地图的气泡、连线，然后气泡的样式
- 插件形式 咱们先开发一些，然后给外部调用

- 切换数据 更新图标
- 热力图
- 地铁图

下钻返回


就像咱们之前做过的 气泡闪烁，或者一个气泡上展示两种数据，上下、左右展现
后续如果有其他人需要不同的，就接着以插件形式往里加
地图上 需要加点击事件，例如点击下钻下一层、返回上一层
地图需要支持渲染，比如根据级别渲染颜色，默认是红橙黄蓝绿五个级别，对应的色值可以跟UI要一份统一的
然后现在弄陕西地铁线路，也是一种特殊的地铁形式，可以单独放进去
国际地图也是如此
就是说可以分为 国际地图、中国地图、地铁线路图，三种地图组件