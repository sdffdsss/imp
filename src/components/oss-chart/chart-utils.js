// 有限度的导出一些图表内部的工具方法
import { convertRectData } from './comp-chart-beta/common';
import { gradientColor } from './utils';

const chartUtils = {
    data: {
        convertRectData,
    },
    gradientColor,
};

export default chartUtils;
